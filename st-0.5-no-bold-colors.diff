diff --git a/st.c b/st.c
index 39d3fee..31fa2e2 100644
--- a/st.c
+++ b/st.c
@@ -3332,7 +3332,7 @@ xdraws(char *s, Glyph base, int x, int y, int charlen, int bytelen) {
 		 * to bright system colors [8-15]
 		 */
 		if(BETWEEN(base.fg, 0, 7) && !(base.mode & ATTR_FAINT))
-			fg = &dc.col[base.fg + 8];
+			fg = &dc.col[base.fg];
 
 		if(base.mode & ATTR_ITALIC) {
 			font = &dc.ibfont;
